# Base dependencies for Project Mikup
# For hardware acceleration, please use:
#   macOS: pip install -r requirements-mac.txt
#   Linux/WSL2 (CUDA): pip install -r requirements-cuda.txt
#   Native Windows (NVIDIA/AMD): pip install -r requirements-windows.txt

# --- Audio Processing & Separation ---
audio-separator==0.41.1
demucs==4.0.1
librosa==0.11.0
soundfile==0.13.1

# --- Machine Learning (March 2026 Stack) ---
torch==2.10.0
torchaudio==2.10.0
torchvision==0.25.0
transformers==5.2.0
torchao==0.7.0
pytorch-lightning==2.6.1
ctranslate2==4.7.1

# --- Transcription & Diarization ---
pyannote.audio==4.0.4
# whisperx==3.8.1  # Commented out due to huggingface-hub version conflict (installed manually)
faster-whisper==1.2.1
# Apple Silicon optional acceleration path (fallback remains faster-whisper).
mlx-whisper; platform_system == "Darwin" and platform_machine == "arm64"

# --- The "AI Director" ---
google-genai==1.65.0
anthropic==0.84.0
openai==2.24.0

# --- Utilities ---
numpy==2.4.2
pandas==2.2.3
pydantic>=2.10.0
scipy==1.17.1
python-dotenv==1.0.1
tqdm==4.67.1
